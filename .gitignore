# Environment and secrets (never commit)
.env

# Dependencies
node_modules/

# Uploads and user data (never overwrite on git pull)
sounds/*.mp3
sounds/*.wav
sounds/*.ogg
sounds/*.webm
sounds/sounds.json

# OS and editor
.DS_Store
Thumbs.db
*.log
.env.local
.env.*.local
