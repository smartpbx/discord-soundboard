# Environment and secrets (never commit)
.env

# Dependencies
node_modules/

# Uploads and user data (local only; never commit or overwrite on pull)
sounds/
data/
guest.json
pending.json
state.json

# OS and editor
.DS_Store
Thumbs.db
*.log
.env.local
.env.*.local

# TTS service
tts-server/.venv/
tts-server/__pycache__/
tts-server/engines/__pycache__/
tts-server/models/
data/tts-cache/

# Secrets / large local-only artifacts (audit 2026-07-20)
models/
yt-profile/
data/yt-profile/
backups/
*.tar.gz
*.bak
companion/config.json
