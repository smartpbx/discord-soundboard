# TTS Service Configuration
HOST=0.0.0.0
PORT=8880

# Device: "cuda" for GPU, "cpu" for CPU-only
# KOKORO_DEVICE=cuda

# GPT-SoVITS API server URL (must be running separately)
# GPT_SOVITS_URL=http://localhost:9880
